import { useState } from 'react'; import Button from '../components/Button'
const tests=[['Hearing test','Plays guided tones. Observe and record the child response.'],['Vision test','Shows an age-appropriate visual prompt.'],['Speech recording','Records only after caregiver consent.']]
export default function AVAssessmentScreen({onNavigate}){const [active,setActive]=useState(null);return <main className="min-h-screen bg-primary-950 p-5"><header className="mx-auto max-w-xl text-white"><button className="min-h-11" onClick={()=>onNavigate('screening')}>← Back</button><h1 className="text-xl font-extrabold">Audio-Visual Assessment</h1></header><section className="mx-auto mt-5 max-w-xl rounded-3xl bg-white p-5"><div className="rounded-xl bg-amber-50 p-3 text-sm text-amber-900"><b>Device integration pending.</b> TODO: hearing, camera, and microphone capture need validated native-device workflows before clinical use. These controls do not generate clinical results.</div><div className="mt-4 space-y-3">{tests.map(([title,detail])=><article key={title} className="rounded-2xl border p-4"><h2 className="font-bold">{title}</h2><p className="mt-1 text-sm text-neutral-500">{detail}</p><Button variant="secondary" className="mt-3 w-full" onClick={()=>setActive(title)}>{active===title?'Selected for assisted assessment':`Start ${title}`}</Button></article>)}</div><Button className="mt-5 w-full" onClick={()=>onNavigate('analysis')}>Run rule-based risk analysis →</Button></section></main>}
